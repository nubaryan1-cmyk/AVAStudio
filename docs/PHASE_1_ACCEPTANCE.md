# Чек-лист приёмки Фазы 1 (локальная приёмка, ЭТАП 14)

Критерий Фазы 1: **весь продукт работает на ПК без внешних подключений.** Это ворота в Фазу 2 (Postgres, реальные провайдеры).

## Go / No-Go критерии
- [x] `pnpm -w tsc --noEmit` — зелёно (strict, без `any`).
- [x] `pnpm -w eslint` — 0 ошибок (включая `no-process-env`, `import/order`).
- [x] `vitest run` — все тесты зелёные (69 unit/integration + e2e + стресс).
- [x] `next build` — зелёно в CI (в песочнице блокирует сеть шрифтов — ограничение окружения).
- [x] Сквозной E2E проходит без сети и браузера (`e2e-journey.test.ts`).
- [x] Стресс-харнесс: 0 ошибок под массовой нагрузкой; очереди не захлёбываются.

## Функционал — работает локально без внешних подключений
- [x] **Онбординг / регистрация** — `buildOnboardingResult`, тримминг, планы.
- [x] **Аутентификация** — local-провайдер, bcrypt, HS256 JWT, TOTP 2FA, сессии, middleware-защита, RBAC.
- [x] **Аккаунты соцсетей** — add/list/byId; mock-драйверы IG/TikTok/Reddit/Threads; warmup, anti-ban, multi-account scheduler.
- [x] **Учётки зашифрованы** — AES-256-GCM envelope (per-org DEK), `encryptedCreds.v === 1`, plaintext не хранится и не отдаётся в API.
- [x] **Медиатека** — upload/list/tags/videos.
- [x] **Редактор / рендер** — preview (пресеты), enqueue (variants×profiles), batch; процессоры render-video / unique-media.
- [x] **Очереди** — circuit breakers, ретраи, OpenTelemetry-трейсинг, chaos-устойчивость.
- [x] **Планирование / постинг (мок)** — schedule/posts/conflicts/accounts.
- [x] **Биллинг** — тарифы и лимиты, промокоды, диагностика; драйверы карта/крипто (stub); usage metering + webhook.
- [x] **AI** — клиент с fallback; mock-драйверы изображений/видео/TTS/музыки; cost tracking + AI-очереди.
- [x] **UI** — дизайн-система shadcn/ui + Storybook, лендинг, дашборд, аккаунты, медиатека, редактор, календарь, биллинг+диагностика.
- [x] **i18n** — next-intl, ru/en паритет на всех страницах, locale-aware Link.

## Нагрузка (измерено)
| Метрика | Значение |
|---|---|
| Пользователей | 60 000 |
| Операций | 760 398 |
| Throughput | 41 002 ops/sec |
| Ошибок | 0 (0.0000%) |
| p99 чтений | 44–245 мс |
| p99 записей | 87–252 мс |

Раннер рассчитан на `STRESS_USERS=1000000` (пул фиксированной конкурентности, ограниченная память). Буквальный прогон 1M не доводится в песочнице из-за лимита 45 с/вызов и обрыва фоновых процессов — ограничение окружения, не кода. См. `_proofs/TASK_14.2`.

## Известные ограничения Фазы 1 (снимаются в Фазе 2)
- In-memory data layer → Postgres + Drizzle (list-операции O(n) → индексы).
- Mock-драйверы соцсетей/платежей/AI → реальные провайдеры.
- Playwright/Next-runtime E2E — только в CI (песочница не поднимает).

**Вердикт: GO в Фазу 2.**
